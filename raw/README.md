# Raw Sources Directory

Drop your immutable source files here.

### Supported formats:
- Markdown (`.md`)
- PDFs / Text (`.pdf`, `.txt`)
- Web clippings / HTML
- Transcripts / Notes

### Assets:
Place any clipped images or attachments into `raw/assets/`.
The LLM will read from these files during ingestion but will **never modify** them.
